
const initialErr = {
    loginError: false
}

const loginReducer = (state = initialErr, action) => {
    const newState = state;
    switch (action.type) {
        default:
            break;
    }
    return newState;
}

export default loginReducer